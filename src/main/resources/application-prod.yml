spring:
    datasource:
        type: com.alibaba.druid.pool.DruidDataSource
        driverClassName: com.mysql.jdbc.Driver
        druid:
            url: jdbc:mysql://115.233.227.46:3336/spider-data?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8
            #            url: jdbc:mysql://localhost:3306/spider?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8
            username: root
            password: 123456
            initial-size: 10
            max-active: 100
            min-idle: 10
            max-wait: 60000
            pool-prepared-statements: true
            max-pool-prepared-statement-per-connection-size: 20
            time-between-eviction-runs-millis: 60000
            min-evictable-idle-time-millis: 300000
            validation-query: SELECT 1 FROM DUAL
            test-while-idle: true
            test-on-borrow: false
            test-on-return: false
            stat-view-servlet:
                enabled: true
                url-pattern: /druid/*
                #login-username: admin
                #login-password: admin
            filter:
                stat:
                    log-slow-sql: true
                    slow-sql-millis: 1000
                    merge-sql: true
                wall:
                    config:
                        multi-statement-allow: true

webmagic:
    selenuim_config: /selenium/config-win.ini

VcCode:
    image:
        #验证码存放位置
        path: C:/spider/vc-image/before/
        #处理过的验证码存放位置
        p-path: C:/spider/vc-image/after/
    chaojiying:
        username: gov123
        password: gov123
        softId: 897003
    #是否使用OCR识别验证码，准确率较低
    useOcr: false